const sequelizeDBConn = require("../database/dbConnection")

const addTaskController = (req, res) => {
    console.log('req', req)
    try {
        //    await sequelizeDBConn. 
    } catch (error) {
        console.log("Error:", error)
    }
}
module.exports = addTaskController